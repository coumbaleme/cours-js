
/*--------------------LES OPERATEURS LOGIQUES------------*/

// l'operateur ET " && "

// si je cumul 2conditions 
// si prenom === loginPrenom et age === loginAge

if((prenom === login) && (age === loginAge)){
    // si ces deux conditions sont remplis alors ici les codes s'execute
}

if (a && b ){// ici il faut deux conditions pour que code s'execute sinon le code ne se lance pas}{}
}

// operateur OU "||"
if(a || b){
// ici il faut au moins une de ces 2 conditions pour que le code s'execute

}

// l'operateur "!" signifie le contraire
let userIsLogin=true
if(userIsLogin){
    // est different de true
    //si la n'est pas connecter, si userI n'est pas connecte, si userIslogin  est false
}
// ce qui revient à ecrire

if(userIsLogin===false){
    // si le user n'est pas connecté, si userIlogin est false
}