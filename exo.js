//concaténer ces variable et afficher dans le dom le resultat


// afficher un promp qui demande l'age si l'age est inferieur à 18 creer une alert de refus sil'age est superier 18 alert d'acceptation

//crée une boucle pour affficher 3 fois une phrase dans le dom
// avec prompt demander au user deux chiffres vous aller stocker les valeurs du prompt puis additionner ces valeur et les afficher dans le dom avec cette phrase:"le resultat est:"

// demander l'age de l'utilisateur avec prompt et verifier s'il peut entre dans une salle de cinema selon les regles suivantes (utimiser console log pour les resultats des conditions)
//--moins de 13ans === accés refuser
//--entre 13 et 17 ans === acces autorise avec accompagnateure
//-- 18 et plus === acces libre



let maxAge=18
let stockagPrompt= Number(prompt("quel est ton age???"))
if(isNaN(stockagPrompt)){
    alert("entrer un nombre correct")
}
else if(stockagPrompt < maxAge){
    alert
}



let n1="nous"
let n2="sommes"
let n3="mercredi"
let n12=12
const  phrase3 = n1 + " " + n2 + " " + n3+ " " +n4+

console.log( phrase3)
const  phrase = n1 + " " + n2 + " " + n3+ " " +n4+

const phrase2 =`${n1} ${n2} ${n3} ${n4}`

for(let i=0 ; i<3;i++){

    document.write("<p>je suis la phrase repeter 3 fois et mon index est:"+i+"</p>")
}
function demanderNombre(message)
let valeurwile (isNaN(valeur))
//
